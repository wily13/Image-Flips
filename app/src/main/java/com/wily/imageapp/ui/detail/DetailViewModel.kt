package com.wily.imageapp.ui.detail

import androidx.lifecycle.ViewModel
import com.wily.imageapp.data.source.ImageRepository

class DetailViewModel(private val mImageRepository: ImageRepository): ViewModel() {
}